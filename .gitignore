node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Image SOURCE material (14MB of full-size jpgs). The processed webp that ships
# lives in static/images/ and is committed; this is what process-images.sh reads
# to produce it, and it does not need to be in the repo or in every Docker build.
/imgs

# Playwright MCP snapshots
.playwright-mcp/
